/**
 * Layout Components - Enforced consistency across all pages
 * 
 * These components are the FOUNDATION of page layout consistency.
 * They are non-negotiable - all pages must use them.
 */

export { PageLayout } from './PageLayout';
export { PageHeader } from './PageHeader';
export { TimelineSection } from './TimelineSection';
export { FooterActions, StandardButton } from './FooterActions';
